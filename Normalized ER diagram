To create a normalized ER diagram that adheres to 1NF, 2NF, and 3NF, we can start by identifying the entities and their attributes based on the provided tables. Here's a suggested solution:

Entities and Attributes:

Bookings:
BookingID (Primary Key)
Date
TableNumber

Orders:
OrderID (Primary Key)
OrderDate
Quantity
TotalCost
TableNumber (Foreign Key referencing Bookings)

OrderDeliveryStatus:
DeliveryStatusID (Primary Key)
OrderID (Foreign Key referencing Orders)
DeliveryDate
Status

Menu:
CuisineID (Primary Key)
CuisineName

Starters:
StarterID (Primary Key)
StarterName
CuisineID (Foreign Key referencing Menu)

Courses:
CourseID (Primary Key)
CourseName
CuisineID (Foreign Key referencing Menu)

Drinks:
DrinkID (Primary Key)
DrinkName
CuisineID (Foreign Key referencing Menu)

Desserts:
DessertID (Primary Key)
DessertName
CuisineID (Foreign Key referencing Menu)

CustomerDetails:
CustomerID (Primary Key)
CustomerName
Phone Number
Email
Address

StaffInformation:
StaffID (Primary Key)
StaffName
Role
Salary

Primary Keys (PK) and Foreign Keys (FK):

Bookings: PK = BookingID
Orders: PK = OrderID, FK = TableNumber
OrderDeliveryStatus: PK = DeliveryStatusID, FK = OrderID
Starters: PK = StarterID, FK = CuisineID
Courses: PK = CourseID, FK = CuisineID
Drinks: PK = DrinkID, FK = CuisineID
Desserts: PK = DessertID, FK = CuisineID
CustomerDetails: PK = CustomerID
StaffInformation: PK = StaffID
Data Types and Constraints:

BookingID: INT (Auto Increment), NOT NULL
Date: DATE, NOT NULL
TableNumber: INT, NOT NULL
OrderID: INT (Auto Increment), NOT NULL
OrderDate: DATE, NOT NULL
Quantity: INT, NOT NULL
TotalCost: DECIMAL, NOT NULL
DeliveryStatusID: INT (Auto Increment), NOT NULL
DeliveryDate: DATE, NOT NULL
Status: VARCHAR, NOT NULL
CuisineID: INT (Auto Increment), NOT NULL
CuisineName: VARCHAR, NOT NULL
StarterID: INT (Auto Increment), NOT NULL
StarterName: VARCHAR, NOT NULL
CourseID: INT (Auto Increment), NOT NULL
CourseName: VARCHAR, NOT NULL
DrinkID: INT (Auto Increment), NOT NULL
DrinkName: VARCHAR, NOT NULL
DessertID: INT (Auto Increment), NOT NULL
DessertName: VARCHAR, NOT NULL
CustomerID: INT (Auto Increment), NOT NULL
CustomerName: VARCHAR, NOT NULL
ContactDetails: VARCHAR, NOT NULL
StaffID: INT (Auto Increment), NOT NULL
StaffName: VARCHAR, NOT NULL
Role: VARCHAR, NOT NULL
Salary: DECIMAL, NOT NULL
